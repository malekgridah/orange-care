server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/malekgridah/orange-care-microservices-config
          default-label: main
        native:
          searchLocations: file:///${GIT_REPO}
