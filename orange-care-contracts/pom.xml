<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.billcom</groupId>
        <artifactId>orange-care-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../orange-care-parent/pom.xml</relativePath>
    </parent>

    <artifactId>orange-care-contracts</artifactId>
    <name>Orange Care :: Contracts</name>
    <description>Bscs Contracts</description>

    <scm>
        <url>${git.url}</url>
        <connection>${git.connection}</connection>
        <developerConnection>${git.developerConnection}</developerConnection>
    </scm>

    <properties>
        <name>Orange Care Contracts</name>
        <main.basedir>${basedir}/..</main.basedir>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <wsi.client>1.0</wsi.client>
        <contract-handling.version>1.0</contract-handling.version>

        <docker.image.exposed.port>8080</docker.image.exposed.port>
        <docker.image.management.port>8081</docker.image.management.port>
        <docker.image.dockerfile.dir>${basedir}/../docker</docker.image.dockerfile.dir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-config-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
        </dependency>
        <dependency>
            <groupId>de.codecentric</groupId>
            <artifactId>spring-boot-admin-starter-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-tomcat</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>contracts-search-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>contract-resources-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>contract-devices-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>services-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>service-packages-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>contract-service-parameters-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>contract-services-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>contract-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>com.billcom</groupId>
            <artifactId>contract-handling</artifactId>
            <version>${contract-handling.version}</version>
        </dependency>
        <dependency>
            <groupId>com.ericsson</groupId>
            <artifactId>rateplans-read-service</artifactId>
            <version>${wsi.client}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-bootstrap</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                            <!-- Build-Info for Info-Actuator -->
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>springboot</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.github.git-commit-id</groupId>
                        <artifactId>git-commit-id-maven-plugin</artifactId>
                        <configuration>
                            <offline>true</offline>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>buildDocker</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
