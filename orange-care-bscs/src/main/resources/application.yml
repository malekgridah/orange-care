spring:
  application:
    name: "@project.artifactId@"
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888/}
  output:
    ansi:
      enabled: always


---
logging:
  level:
    com.billcom: debug
